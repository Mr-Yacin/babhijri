---
import Layout from "../../../layouts/Layout.astro";
import Header from "../../../components/common/Header.astro";
import Footer from "../../../components/common/Footer.astro";
import UserList from "../../../components/admin/UserList.svelte";
import { SITE_CONFIG } from "../../../config";
---

<script>
    import { requireAdmin } from "../../../lib/utils/auth-guard";
    requireAdmin();
</script>

<Layout title={`إدارة المستخدمين | ${SITE_CONFIG.name}`}>
    <Header />
    <main class="min-h-screen bg-gray-50" dir="rtl">
        <section
            class="bg-gradient-to-r from-indigo-900 to-indigo-700 text-white py-12"
        >
            <div class="container mx-auto px-4">
                <h1 class="text-3xl md:text-4xl font-bold mb-2">
                    إدارة المستخدمين
                </h1>
                <p class="text-indigo-100 text-lg">
                    قائمة جميع المستخدمين مع أدوات الإدارة
                </p>
            </div>
        </section>

        <section class="container mx-auto px-4 py-8">
            <UserList client:only="svelte" />
        </section>
    </main>
    <Footer />
</Layout>
