---
import Layout from "../../../../layouts/Layout.astro";
import Header from "../../../../components/common/Header.astro";
import Footer from "../../../../components/common/Footer.astro";
import UserDetail from "../../../../components/admin/UserDetail.svelte";
import { SITE_CONFIG } from "../../../../config";

const { uid } = Astro.params;
---

<script>
    import { requireAdmin } from "../../../../lib/utils/auth-guard";
    requireAdmin();
</script>

<Layout title={`تفاصيل المستخدم | ${SITE_CONFIG.name}`}>
    <Header />
    <main class="min-h-screen bg-gray-50" dir="rtl">
        <!-- Header -->
        <section class="bg-white border-b border-gray-200 py-8">
            <div class="container mx-auto px-4">
                <div class="flex items-center gap-2 text-sm text-gray-500 mb-4">
                    <a href="/app/admin" class="hover:text-indigo-600"
                        >لوحة الإدارة</a
                    >
                    <span>/</span>
                    <a href="/app/admin/users" class="hover:text-indigo-600"
                        >المستخدمين</a
                    >
                    <span>/</span>
                    <span class="text-gray-900">تفاصيل المستخدم</span>
                </div>
                <h1 class="text-2xl font-bold text-gray-900">ملف المستخدم</h1>
            </div>
        </section>

        <div class="container mx-auto px-4 py-8">
            <UserDetail client:only="svelte" uid={uid as string} />
        </div>
    </main>
    <Footer />
</Layout>
